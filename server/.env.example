PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/bearing-erp
JWT_SECRET=change_this_to_a_long_random_string
JWT_EXPIRES_IN=7d
CLIENT_ORIGIN=http://localhost:5173

# Gmail SMTP for password-reset emails (16-character Gmail App Password, not the account password)
GMAIL_USER=
GMAIL_APP_PASSWORD=

# Company and bank details for invoices are hardcoded in src/config/company.js
